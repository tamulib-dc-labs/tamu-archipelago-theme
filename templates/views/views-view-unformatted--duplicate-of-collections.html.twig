{#
/**
 * Wraps solr_search_collections view rows in a .tamu-cards-grid container.
 */
#}
{% if title %}
  <h3>{{ title }}</h3>
{% endif %}

<div class="tamu-cards-grid w-100">
  {% for row in rows %}
    <div>{{ row.content }}</div>
  {% endfor %}
</div>
